module.exports = function (answers) {

    function bordaMath(surveyid, answers) {
        // Get anwser id from DB
        // let surveyID = 

        // Verify anwser position on drag-and-drop
        // jquery

        // Make the math
        // get 1st position of drag-and drop
        // for (let i = 0; i < answers.length - 1; i++ )
            //_edit.database(survey id x. answers id y .SUM(answers[i]))
        // Call the function to save the SUM of the new number to the database

    }
}